const express= require('express')
const app= require()
app.use(express.json())

// app.use('/au',require('./Routes/attributes'))
// app.use('cat',require('./Routes/categories'))
// app.use('/cus',require('./Routes/customers'))
// app.use('dep',require('./Routes/departments'))
// app.use('ord',require('./Routes/orders'))
// app.use('pro',require('./Routes/products'))
// app.use('shi',require('./Routes/shipping'))
// app.use('sho',require('./Routes/shoppingcart'))
// app.use('ta',require('./Routes/tax'))

app.listen(2020,()=>{
    console.log("server start")
});






